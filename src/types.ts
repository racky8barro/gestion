// src/types.ts

export interface Income {
    id: number;
    title: string;
    amount: string;
  }
  
  export interface Expense {
    id: number;
    title: string;
    amount: string;
  }
  
  // Ajoutez d'autres interfaces si nécessaire
  